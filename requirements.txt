# --- Data Generation & Utilities ---
# Used to access Gemini for generating synthetic Swiss banking data
google-genai==0.3.0
# Handles environment variables to keep the API Key secure
python-dotenv==1.2.1
# Shows progress bars during long data generation or evaluation loops
tqdm==4.67.1
# Used for HTTP requests and API calls (e.g., interacting with local Ollama)
requests==2.32.3

# --- Data Validation & Annotation (Manual Review) ---
# The main tool for manually checking NER tag accuracy
label-studio==1.15.0
# Must be exactly 1.0.8 for Label Studio 1.15.0
label-studio-sdk==1.0.8

# --- Data Analysis & Visualization ---
# Used to calculate metrics (Precision/Recall) and organize results
pandas==2.2.3
# For Exporting to Excel
openpyxl==3.1.5
# REQUIRED: Label Studio 1.15.0 needs >= 1.26.4. (Compatible with Torch/spaCy)
numpy==1.26.4
# Formats the results into pretty tables for the terminal and LaTeX
tabulate==0.9.0
# Generates charts and plots for the comparative analysis (RQ1/RQ2)
matplotlib==3.10.0
# Statistical data visualization (used for academic-grade thesis charts)
seaborn==0.13.2

# --- Classical Baseline ---
# Industrial-strength NLP for classical NER baseline and regex tokenization
spacy==3.8.0

# --- PII Detection & Anonymization ---
# Microsoft Presidio for analyzing and anonymizing PII data
presidio-analyzer==2.2.361
presidio-anonymizer==2.2.361

# --- LLM Evaluation & Anonymization ---
# Deep learning backend required by bert-score and transformers
torch==2.5.1
# Hugging Face library needed for loading metric models
transformers==4.48.2
# Calculates semantic similarity between anonymized text and original
bert-score==0.3.13

# --- Memory Optimization & Quantization ---
# Manages device mapping to efficiently load large models into GPU memory
accelerate==1.13.0
# Enables 4-bit and 8-bit quantization to fit 13B+ and 70B models on 24GB VRAM
bitsandbytes==0.49.2

# --- QLoRA Fine-Tuning & NER Evaluation ---
# Parameter-Efficient Fine-Tuning (provides LoRA adapters)
peft==0.18.1
# Transformer Reinforcement Learning (provides SFTTrainer for easy fine-tuning)
trl==0.29.0
# Standard Hugging Face library for managing and loading training datasets
datasets==4.7.0
# Standard framework for calculating exact-match F1 scores on NER tagging tasks
seqeval==1.2.2